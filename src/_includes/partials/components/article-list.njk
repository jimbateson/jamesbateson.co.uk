{% if postListItems.length %}
<section class="[ post-list ] [ pad-top-400 pad-bottom-400 ]" aria-labelledby="articles-heading">
	<div class="[ wrapper ] [ sf-flow ]">
		{% if postListHeading %}
			<h2 class="[ post-list__heading ] [ text-700 md:text-max ]" id="articles-heading">{{ postListHeading }}</h2>
			<p class="section-intro">{{ postListSummary }}</p>
		{% endif %}

		<ol class="[ post-list__items ] [ sf-flow ] [ pad-top-500 ]" reversed>
			{% for item in postListItems %}
				<li class="post-list__item">
					<h3 class="[ leading-tight text-600 md:text-800 ]">
						<a href="{{ item.url }}" class="post-list__link link-hover" rel="bookmark">{{ item.data.title }}</a>
					</h3>
					<p class="gap-top-300">
						<time datetime="{{ item.date | w3DateFilter }}">{{ item.date | dateFilter }}</time>
					</p>
				</li>
			{% endfor %}
		</ol>

		{% if page.url == '/' %}
		<a href="/articles" class="[ post-list__archive ] [ text-500 ]">See all articles</a>
		{% endif %}
    </div>
</section>
{% endif %}
